import React from 'react';
import Card from '../Card/Card'

const Favorites = (props) => {
  console.log(props)
  return (
    <div>
      Favorites
      <Card />
    </div>
  )
}

export default Favorites