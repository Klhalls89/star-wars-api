import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

describe('App', () => {

  describe('componentDidMount', () => {
    mockResults = {results:['movie', 'movies']}

    it('should call getMovies', () => {

    })

    it('should call  setMovie with correct prams', async () => {
      await getMovies()


    })

  })

})
